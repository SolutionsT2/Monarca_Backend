JWT_SECRET=monarca-dev-jwt-secret-not-for-production
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=
DOWNLOAD_LINK=
FRONTEND_URL=

# Email SMTP (MailHog for dev: host=localhost, port=1025)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASSWORD=
DUFFEL_API_KEY=
DUFFEL_TIMEOUT_MS=130000

# Policy engine (test only): allow vouchers with dates before trip start.
# Keep false/empty in production.
ALLOW_PRETRIP_VOUCHER_DATES_FOR_TESTS=false

# Policy engine (test only): bypass amount threshold operators (LT/LTE/GT/GTE).
# Keep false/empty in production.
ALLOW_VOUCHER_AMOUNT_RULE_BYPASS_FOR_TESTS=false

# BANXICO_TOKEN
BANXICO_TOKEN=