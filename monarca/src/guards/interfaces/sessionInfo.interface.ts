/**
 * File: sessionInfo.interface.ts
 * Description: Shape of the session payload attached to the request after JWT validation (e.g. by AuthGuard).
 */

export interface SessionInfoInterface {
  id: string;
}

/**
 * Modification History:
 * - 2026-03-02: Added file header with description and modification history.
 */
